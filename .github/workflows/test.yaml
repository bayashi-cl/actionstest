# name: test
# on: push
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     setps:
#       - name: echos
#         run: |
#           echo "Hello acions!!"
#           pwd
name: GitHub Actions Demo
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
          pwd
